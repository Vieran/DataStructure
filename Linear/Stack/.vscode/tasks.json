{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "options": {
        "cwd": "${workspaceRoot}/build"
    },//必须加上，不然编译出问题？
    "tasks": [
        {//给cmake的
            "label": "cmake",
            "type": "shell",
            "command": "cmake",
            "args": [
                "-g",
                "-o",
                "${fileDirname}/build/${workspaceRootFolderName}.exe",
                "${file}"
                ],//前是output（最后一个/后是可执行程序），后是input
        },
        {//给make的
            "label": "make",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "type": "shell",
            "command":"make",
            "args": []
        }
    ]
}

//${fileDirname}   当前打开的文件绝对路径，不包括文件名
//${fileExtname} 当前打开的文件的后缀名
//${fileBasenameNoExtension}	当前文件的文件名(包括绝对路径、文件名)
//${relativeFile}	当前文件的相对路径（从当前文件夹到当前文件）
//${file}	当前正在编辑的文件的完整文件名（包括绝对路径、文件名、后缀名）
//${workspaceRootFolderName}   当前打开的文件夹的名字